# 프론트엔드 아키텍처 설명

## 기본 정보

- **프레임워크**: React 19 + TypeScript + Vite 8
- **아키텍처**: FSD (Feature-Sliced Design) 변형
- **배포**: AWS S3 + CloudFront 정적 배포
- **로컬 포트**: 5173

---

## 디렉토리 구조

```
parantier-front/src/
├── app/
│   └── routes/index.tsx      # TanStack Router 전체 라우트 정의
├── pages/                    # 페이지 컴포넌트 (라우트 1:1 매핑)
│   ├── admin/                # 관리자 페이지 (users, menus, roles 등)
│   ├── main/                 # 대시보드
│   ├── work/                 # 업무 관리
│   ├── study/                # 스터디
│   ├── wiki/                 # Tech Wiki
│   ├── faq/                  # FAQ
│   ├── meeting/              # 회의실
│   ├── sql/                  # SQL 연습장
│   ├── subutai/              # Subutai AI (AI/FAQ/Docu)
│   ├── backend/              # Backend 문서 (카테고리+섹션+블록)
│   ├── frontend/             # Frontend 문서
│   ├── architecture/         # 아키텍처 문서
│   ├── figma/                # Figma 문서
│   ├── devlog/               # 개발 일지
│   ├── hackathon/            # 해커톤
│   └── ...
├── features/                 # 기능별 로직 (API, 훅, 컴포넌트)
│   ├── task/                 # 문서 관리 기능
│   │   ├── api/taskApi.ts
│   │   ├── hooks/useTask.ts
│   │   ├── types/task.types.ts
│   │   └── components/TasksPage.tsx, TaskBlockEditor.tsx, TaskBlockViewer.tsx
│   ├── auth/                 # 인증
│   ├── study/                # 스터디
│   ├── subutai-docu/         # Subutai 문서 관리
│   ├── subutai-faq/          # Subutai FAQ
│   └── ...
├── entities/
│   ├── user/model/authStore.ts    # 인증 상태 (TanStack Store)
│   ├── menu/model/              # 메뉴 상태
│   └── ui/model/navThemeStore.ts
├── shared/
│   ├── ui/                   # 공통 UI 컴포넌트
│   │   ├── shadcn/           # shadcn/ui 컴포넌트
│   │   ├── custom/           # 커스텀 컴포넌트
│   │   ├── lexical/          # Lexical 에디터
│   │   └── mermaid/          # Mermaid 다이어그램
│   ├── hooks/                # 공통 훅 (useConfirm 등)
│   ├── api/client.ts         # Axios 클라이언트 (baseURL 설정)
│   └── lib/                  # 유틸리티
└── widgets/
    └── header/Header.tsx     # 상단 네비게이션
```

---

## 라우팅 (TanStack Router)

`src/app/routes/index.tsx`에 모든 라우트 정의

```typescript
const coreRoute = createRoute({
  getParentRoute: () => rootRoute,
  path: "/backend",
  beforeLoad: () => requireAuth(),  // 인증 체크
  component: BackendPage,
});

// routeTree에 추가
const routeTree = rootRoute.addChildren([
  dashboardRoute,
  backendRoute,
  subutaiAiRoute,    // /subutai/ai
  subutaiFaqRoute,   // /subutai/faq
  subutaiDocuRoute,  // /subutai/docu
  adminUsersRoute,   // ROLE_ADMIN 전용
  ...
]);
```

### 권한 체크 패턴
```typescript
// 로그인 필요
const requireAuth = async () => {
  await waitForRestore();
  if (!authStore.state.isAuthenticated) {
    throw redirect({ to: "/dashboard" });
  }
};

// 관리자 필요
const requireRole = async (requiredRole: string) => {
  await waitForRestore();
  if (authStore.state.user?.role !== requiredRole) {
    throw redirect({ to: "/dashboard" });
  }
};
```

---

## 상태 관리

### 인증 상태 (TanStack Store)
```typescript
// entities/user/model/authStore.ts
export const authStore = new Store({
  user: null,
  isAuthenticated: false,
  isRestored: false,
  isLoginModalOpen: false,
});

// 사용
const { user, isAuthenticated } = useStore(authStore, (s) => s);
const isAdmin = user?.role === "ROLE_ADMIN";
```

### 서버 상태 (TanStack Query)
```typescript
// features/task/hooks/useTask.ts
export const useTaskFolders = () =>
  useQuery({ queryKey: ["taskFolders"], queryFn: taskApi.getFolders });

export const useSaveTaskMutation = () =>
  useMutation({ mutationFn: taskApi.savePost,
    onSuccess: () => queryClient.invalidateQueries(["taskPosts"]) });
```

---

## API 호출 패턴

### Axios 클라이언트 (shared/api/client.ts)
```typescript
export const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_URL + "/api",
  headers: { "Content-Type": "application/json" },
});

// 요청 인터셉터: JWT 자동 첨부
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem("accessToken");
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});
```

### Native Fetch 패턴 (일부 기능)
```typescript
const getAuthHeaders = () => ({
  "Content-Type": "application/json",
  Authorization: `Bearer ${localStorage.getItem("accessToken") ?? ""}`,
});

export const faqApi = {
  getCategories: (): Promise<FaqCategory[]> =>
    fetch(`${BASE}/faq/categories`, { headers: getAuthHeaders() })
      .then((r) => r.json()),
};
```

---

## 다크모드

- `next-themes` 기반 (class 방식)
- `tailwind.config.ts`: `darkMode: ["class", ".dark"]`
- CSS 변수로 라이트/다크 토큰 정의 (`globals.css`)

### 필수 규칙: 시맨틱 색상 토큰만 사용
```tsx
// ✅ 올바른 사용
className="bg-background text-foreground"
className="bg-card text-card-foreground"
className="bg-muted text-muted-foreground"
className="border-border"
className="bg-primary text-primary-foreground"
className="bg-destructive text-destructive-foreground"

// ❌ 하드코딩 금지
className="bg-white text-black"
className="bg-gray-100"
```

---

## 주요 페이지 목록

| 경로 | 페이지 | 설명 |
|------|--------|------|
| `/` | MainPage | 대시보드 |
| `/docs` | TasksPage | 문서 관리 (폴더+게시글+블록) |
| `/wiki` | WikiPage | Tech Wiki |
| `/work` | WorkPage | 업무 관리 |
| `/study` | StudyPage | 스터디 |
| `/faq` | FaqPage | FAQ |
| `/meeting` | MeetingPage | 회의실 |
| `/chat` | ChatRoomsPage | 채팅방 |
| `/sql` | SqlPage | SQL 연습장 |
| `/backend` | BackendPage | Backend 문서 |
| `/frontend` | FrontendPage | Frontend 문서 |
| `/subutai/ai` | SubutaiAiPage | AI 챗봇 |
| `/subutai/faq` | SubutaiFaqPage | Subutai FAQ |
| `/subutai/docu` | SubutaiDocuPage | Subutai 문서 |
| `/admin/users` | UsersPage | 유저 관리 (ADMIN) |
| `/admin/menus` | MenusPage | 메뉴 관리 (ADMIN) |

---

## 블록 에디터 (TaskBlockEditor)

문서의 컨텐츠를 블록 단위로 편집하는 핵심 컴포넌트

```
BlockType:
├── NOTE    → Lexical 리치 텍스트 에디터
├── MMD     → Mermaid 다이어그램 편집/미리보기
├── FIGMA   → Figma 임베드 URL 입력
├── FILE    → S3 파일 업로드/다운로드
├── DBTABLE → DB 테이블 정의 편집
└── GITHUB  → GitHub 링크 카드
```

---

## Header 컴포넌트

`widgets/header/Header.tsx`

- DB에서 메뉴 목록 fetch (동적 네비게이션)
- 로그인/로그아웃 Popover
- 다크모드 토글 (ModeToggle)
- 권한에 따라 메뉴 필터링

---

## 환경 변수

```env
VITE_API_URL=http://localhost:8080   # 로컬
VITE_API_URL=https://api.dxline-tallent.com  # 프로덕션
```
