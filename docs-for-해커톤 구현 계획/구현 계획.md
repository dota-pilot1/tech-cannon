# 해커톤 페이지 구현 계획

## 현재 구현 상태 (Phase 1 완료)

### 페이지 구조
- 라우트: `/hackathon`
- 파일: `parantier-front/src/pages/hackathon/HackathonPage.tsx`
- 메뉴: 헤더 HEADER 타입으로 등록 (id: 55)

### 현재 UI 구성
```
┌─────────────────────────────────────────────────────────────┐
│ 상단 요약 카드 4개: 참가팀 / 참가인원 / 남은시간 / 채팅수    │
├──────────────────┬──────────────────────────────────────────┤
│  왼쪽: 오픈채팅  │  오른쪽: Team A 카드 │ Team B 카드       │
│  (w-72 고정)     │  탭: Figma / Task / Issue / GitHub / FAQ  │
└──────────────────┴──────────────────────────────────────────┘
```

### 구현된 기능
- [x] 페이지 라우터 등록 (`/hackathon`)
- [x] 상단 요약 카드 (참가팀, 인원, 시간, 채팅수)
- [x] 왼쪽 오픈 채팅 UI (더미 데이터, Enter/Send 전송)
- [x] 오른쪽 2팀 카드 가로 배치 (Team A / Team B)
- [x] 팀별 색상 테마 (A: 파랑, B: 초록)
- [x] 탭 UI 골격 (Figma / Task / Issue / GitHub / FAQ)
- [x] 각 탭 빈 상태 + "구현 예정" 플레이스홀더

---

## Phase 2 - DB 설계 및 백엔드 API

### 2-1. DB 테이블 설계

#### `hackathon_events` - 해커톤 이벤트
```sql
CREATE TABLE hackathon_events (
    id          BIGSERIAL PRIMARY KEY,
    title       VARCHAR(200) NOT NULL,           -- "2024 사내 해커톤"
    description TEXT,
    start_at    TIMESTAMP NOT NULL,              -- 시작 시간
    end_at      TIMESTAMP NOT NULL,              -- 종료 시간
    max_teams   INTEGER NOT NULL DEFAULT 2,      -- 최대 팀 수 (현재 2팀 제한)
    is_active   BOOLEAN NOT NULL DEFAULT true,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `hackathon_teams` - 팀 정보
```sql
CREATE TABLE hackathon_teams (
    id          BIGSERIAL PRIMARY KEY,
    event_id    BIGINT NOT NULL REFERENCES hackathon_events(id) ON DELETE CASCADE,
    name        VARCHAR(100) NOT NULL,           -- "Team A", "Team B"
    project     VARCHAR(300),                    -- 프로젝트명
    color_theme VARCHAR(50),                     -- "blue" | "emerald"
    order_num   INTEGER NOT NULL DEFAULT 0,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `hackathon_team_members` - 팀 멤버
```sql
CREATE TABLE hackathon_team_members (
    id       BIGSERIAL PRIMARY KEY,
    team_id  BIGINT NOT NULL REFERENCES hackathon_teams(id) ON DELETE CASCADE,
    user_id  BIGINT NOT NULL REFERENCES users(id),
    joined_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE (team_id, user_id)
);
```

#### `hackathon_chat_messages` - 오픈 채팅
```sql
CREATE TABLE hackathon_chat_messages (
    id         BIGSERIAL PRIMARY KEY,
    event_id   BIGINT NOT NULL REFERENCES hackathon_events(id) ON DELETE CASCADE,
    user_id    BIGINT NOT NULL REFERENCES users(id),
    username   VARCHAR(100) NOT NULL,
    content    TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_hackathon_chat_event_id ON hackathon_chat_messages (event_id, created_at DESC);
```

#### `hackathon_team_links` - 팀별 링크 (Figma, GitHub 등)
```sql
CREATE TABLE hackathon_team_links (
    id         BIGSERIAL PRIMARY KEY,
    team_id    BIGINT NOT NULL REFERENCES hackathon_teams(id) ON DELETE CASCADE,
    link_type  VARCHAR(20) NOT NULL,             -- 'figma' | 'github' | 'notion' | 'custom'
    title      VARCHAR(200),
    url        TEXT NOT NULL,
    created_by BIGINT REFERENCES users(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `hackathon_team_tasks` - 팀별 Task (기존 Task 연동 or 독립)
```sql
-- 옵션 1: 기존 task_posts 테이블 연동 (task_id 참조)
-- 옵션 2: 독립 테이블로 경량화
CREATE TABLE hackathon_team_tasks (
    id          BIGSERIAL PRIMARY KEY,
    team_id     BIGINT NOT NULL REFERENCES hackathon_teams(id) ON DELETE CASCADE,
    title       VARCHAR(300) NOT NULL,
    status      VARCHAR(20) NOT NULL DEFAULT 'todo',  -- todo | doing | done
    assignee_id BIGINT REFERENCES users(id),
    due_at      TIMESTAMP,
    created_by  BIGINT REFERENCES users(id),
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `hackathon_team_issues` - 팀별 이슈
```sql
CREATE TABLE hackathon_team_issues (
    id          BIGSERIAL PRIMARY KEY,
    team_id     BIGINT NOT NULL REFERENCES hackathon_teams(id) ON DELETE CASCADE,
    title       VARCHAR(300) NOT NULL,
    content     TEXT,
    status      VARCHAR(20) NOT NULL DEFAULT 'open',  -- open | in_progress | closed
    priority    VARCHAR(10) NOT NULL DEFAULT 'medium', -- low | medium | high
    assignee_id BIGINT REFERENCES users(id),
    created_by  BIGINT REFERENCES users(id),
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### `hackathon_team_faq` - 팀별 FAQ
```sql
CREATE TABLE hackathon_team_faq (
    id         BIGSERIAL PRIMARY KEY,
    team_id    BIGINT NOT NULL REFERENCES hackathon_teams(id) ON DELETE CASCADE,
    question   TEXT NOT NULL,
    answer     TEXT,
    order_num  INTEGER NOT NULL DEFAULT 0,
    created_by BIGINT REFERENCES users(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

### 2-2. 백엔드 API 설계

#### 이벤트 API (`/api/hackathon`)
| Method | Path | 설명 | 권한 |
|--------|------|------|------|
| GET | `/api/hackathon/events/active` | 현재 진행중 이벤트 조회 | 인증 |
| POST | `/api/hackathon/events` | 이벤트 생성 | ADMIN |
| PUT | `/api/hackathon/events/{id}` | 이벤트 수정 | ADMIN |

#### 팀 API
| Method | Path | 설명 | 권한 |
|--------|------|------|------|
| GET | `/api/hackathon/events/{eventId}/teams` | 팀 목록 조회 | 인증 |
| POST | `/api/hackathon/events/{eventId}/teams` | 팀 생성 | ADMIN |
| PUT | `/api/hackathon/teams/{id}` | 팀 정보 수정 | ADMIN |
| POST | `/api/hackathon/teams/{id}/members` | 팀원 추가 | ADMIN |
| DELETE | `/api/hackathon/teams/{id}/members/{userId}` | 팀원 제거 | ADMIN |

#### 채팅 API + WebSocket
| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/hackathon/events/{eventId}/chat?limit=50` | 최근 채팅 조회 |
| WS | `hackathon-chat/{eventId}` | 실시간 채팅 |

#### 링크 API
| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/hackathon/teams/{id}/links` | 링크 목록 |
| POST | `/api/hackathon/teams/{id}/links` | 링크 추가 |
| DELETE | `/api/hackathon/teams/{id}/links/{linkId}` | 링크 삭제 |

#### Task API
| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/hackathon/teams/{id}/tasks` | Task 목록 |
| POST | `/api/hackathon/teams/{id}/tasks` | Task 추가 |
| PUT | `/api/hackathon/teams/{id}/tasks/{taskId}` | Task 수정 |
| DELETE | `/api/hackathon/teams/{id}/tasks/{taskId}` | Task 삭제 |

#### Issue API
| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/hackathon/teams/{id}/issues` | 이슈 목록 |
| POST | `/api/hackathon/teams/{id}/issues` | 이슈 등록 |
| PUT | `/api/hackathon/teams/{id}/issues/{issueId}` | 이슈 수정 |

#### FAQ API
| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/hackathon/teams/{id}/faq` | FAQ 목록 |
| POST | `/api/hackathon/teams/{id}/faq` | FAQ 추가 |
| PUT | `/api/hackathon/teams/{id}/faq/{faqId}` | FAQ 수정 |
| DELETE | `/api/hackathon/teams/{id}/faq/{faqId}` | FAQ 삭제 |

---

## Phase 3 - 프론트엔드 기능 구현

### 3-1. 채팅 실시간화
- 현재: 더미 데이터 (로컬 state)
- 목표: WebSocket 연동 (기존 `usePureWebSocket` 패턴 재사용)
- 토픽: `hackathon-chat/{eventId}`
- 히스토리: REST API로 초기 50개 로드

### 3-2. 팀 카드 탭 기능 구현

#### Figma 탭
- Figma URL 등록/수정/삭제
- iframe 미리보기 or 링크 카드
- 여러 Figma 파일 등록 가능

#### Task 탭
- 상태별 칸반 뷰: `TODO / DOING / DONE`
- 담당자 지정, 마감일 설정
- 드래그로 상태 변경 (dnd-kit 활용)

#### Issue 탭
- 이슈 목록 (우선순위 배지: 🔴높음 / 🟡중간 / 🟢낮음)
- 상태: OPEN / IN_PROGRESS / CLOSED
- 이슈 상세 모달

#### GitHub 탭
- GitHub 레포 URL 연결
- 레포 링크 카드 표시
- PR/커밋 연동 (GitHub API, 선택적)

#### FAQ 탭
- Q&A 아코디언 목록
- ADMIN이 질문/답변 등록
- 순서 변경 (DnD)

### 3-3. 타이머
- 해커톤 종료 시간까지 카운트다운 실시간 표시
- `HH:MM:SS` 형식
- 종료 시 "해커톤 종료!" 표시

### 3-4. 팀 참가 신청
- 팀원 모집 중인 팀에 참가 신청 버튼
- ADMIN이 승인/거절
- 팀별 최대 인원 설정

---

## Phase 4 - 고도화

### 4-1. 결과 발표
- 팀별 최종 발표 링크 (YouTube, Notion 등)
- 투표 기능 (참가자 인기상)
- 결과 보관 아카이브

### 4-2. 진행률 트래킹
- 팀별 Task 완료율 자동 계산 → 진행률 바
- 실시간 업데이트

### 4-3. 알림
- 채팅 새 메시지 알림
- Task 마감 임박 알림
- Issue 담당자 지정 알림

---

## 백엔드 도메인 구조 (예정)

```
hackathon/
├── domain/
│   ├── HackathonEvent.java
│   ├── HackathonTeam.java
│   ├── HackathonTeamMember.java
│   ├── HackathonChatMessage.java
│   ├── HackathonTeamLink.java
│   ├── HackathonTeamTask.java
│   ├── HackathonTeamIssue.java
│   └── HackathonTeamFaq.java
├── dto/
│   ├── HackathonEventResponse.java
│   ├── HackathonTeamResponse.java
│   └── ...
├── infrastructure/
│   ├── HackathonEventMapper.java
│   ├── HackathonTeamMapper.java
│   └── ...
├── application/
│   ├── HackathonEventService.java
│   └── HackathonTeamService.java
└── presentation/
    ├── HackathonEventController.java
    └── HackathonTeamController.java
```

## MyBatis XML 테이블 매핑
```
resources/mybatis/mapper/
├── HackathonEventMapper.xml
├── HackathonTeamMapper.xml
├── HackathonChatMapper.xml
├── HackathonTaskMapper.xml
├── HackathonIssueMapper.xml
└── HackathonFaqMapper.xml
```

---

## 프론트엔드 파일 구조 (예정)

```
features/hackathon/
├── api/
│   └── hackathonApi.ts          # REST API 모듈
├── hooks/
│   └── useHackathonChat.ts      # WebSocket 채팅 훅
├── types/
│   └── hackathon.types.ts       # 타입 정의
└── components/
    ├── TeamCard.tsx              # 팀 카드 컴포넌트
    ├── HackathonChat.tsx         # 채팅 컴포넌트
    ├── HackathonTimer.tsx        # 카운트다운 타이머
    ├── tabs/
    │   ├── FigmaTab.tsx
    │   ├── TaskTab.tsx
    │   ├── IssueTab.tsx
    │   ├── GithubTab.tsx
    │   └── FaqTab.tsx
    └── modals/
        ├── AddTaskModal.tsx
        ├── AddIssueModal.tsx
        └── AddFaqModal.tsx

pages/hackathon/
└── HackathonPage.tsx             # 메인 페이지 (현재 구현 중)
```

---

## 구현 우선순위

| 우선순위 | 기능 | 예상 공수 |
|---------|------|---------|
| 🔴 높음 | DB 테이블 생성 + 백엔드 기본 CRUD | 1일 |
| 🔴 높음 | 팀 정보 실제 데이터 연동 | 0.5일 |
| 🔴 높음 | 채팅 WebSocket 실시간화 | 0.5일 |
| 🟡 중간 | Figma 링크 등록/표시 | 0.5일 |
| 🟡 중간 | Task 칸반 뷰 | 1일 |
| 🟡 중간 | Issue 관리 | 1일 |
| 🟡 중간 | 타이머 카운트다운 | 0.5일 |
| 🟢 낮음 | GitHub 탭 연동 | 0.5일 |
| 🟢 낮음 | FAQ 탭 | 0.5일 |
| 🟢 낮음 | 팀 참가 신청 | 1일 |
| 🟢 낮음 | 결과 발표 / 투표 | 2일 |

---

## 참고 - 기존 패턴 재사용

- WebSocket: `usePureWebSocket` (회의실 채팅 패턴 동일)
- REST API: `architectureApi.ts` 패턴 동일
- 칸반 DnD: `@dnd-kit/core` (아키텍처 페이지 패턴)
- 블록 에디터: `TaskBlockEditor` (FAQ 내용 작성 시)
- 인증: `@AuthenticationPrincipal User user` + `isAdmin` 체크