# 리프레시 토큰 구현 계획

## 개요

현재 액세스 토큰만 사용하는 기본 JWT 인증을 **액세스 토큰 + 리프레시 토큰** 구조로 개선합니다.

**목표**:
1. 보안성 향상 (짧은 액세스 토큰 만료 시간)
2. 사용자 경험 개선 (긴 로그인 유지)
3. 토큰 무효화 기능 추가 (로그아웃, 보안 이벤트 대응)

---

## 구현 단계

### Phase 1: 데이터베이스 설계 및 도메인 모델
### Phase 2: JWT 토큰 제공자 확장
### Phase 3: 서비스 레이어 구현
### Phase 4: API 엔드포인트 추가
### Phase 5: 예외 처리 및 보안 강화
### Phase 6: 테스트 및 검증

---

## Phase 1: 데이터베이스 설계 및 도메인 모델

### 1-1. refresh_tokens 테이블 생성

**파일**: `src/main/resources/db/migration/V3__create_refresh_tokens_table.sql`

```sql
CREATE TABLE refresh_tokens (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    token VARCHAR(500) NOT NULL,
    expiry_date TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT fk_refresh_token_user
        FOREIGN KEY (user_id)
        REFERENCES users(id)
        ON DELETE CASCADE
);

-- 인덱스 추가 (성능 최적화)
CREATE INDEX idx_refresh_token_token ON refresh_tokens(token);
CREATE INDEX idx_refresh_token_user_id ON refresh_tokens(user_id);

-- 만료된 토큰 정리를 위한 인덱스
CREATE INDEX idx_refresh_token_expiry_date ON refresh_tokens(expiry_date);
```

**설계 포인트**:
- `user_id`: 어떤 사용자의 토큰인지 식별
- `token`: 실제 리프레시 토큰 문자열 (JWT)
- `expiry_date`: 만료 시각 (7일 ~ 30일 후)
- `created_at`: 발급 시각 (로그 추적용)
- `ON DELETE CASCADE`: 사용자 삭제 시 토큰도 자동 삭제

**테이블 용량 계산**:
```
- 사용자 1명당 1개의 리프레시 토큰
- 토큰 문자열: 약 300 bytes
- 메타데이터: 약 50 bytes
- 1만 명 사용자: 약 3.5MB
- 100만 명 사용자: 약 350MB
→ 용량 부담 적음
```

---

### 1-2. RefreshToken 도메인 엔티티 생성

**파일**: `src/main/java/com/mapo/palantier/user/domain/RefreshToken.java`

```java
package com.mapo.palantier.user.domain;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;
import lombok.NoArgsConstructor;

import java.time.LocalDateTime;

/**
 * 리프레시 토큰 도메인 모델
 * - 액세스 토큰 재발급을 위한 장기 토큰
 * - DB에 저장하여 관리 (무효화 가능)
 */
@Getter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class RefreshToken {
    private Long id;
    private Long userId;           // User 엔티티의 ID
    private String token;          // JWT 리프레시 토큰
    private LocalDateTime expiryDate;
    private LocalDateTime createdAt;

    /**
     * 팩토리 메서드: 새 리프레시 토큰 생성
     */
    public static RefreshToken create(Long userId, String token, int validDays) {
        return RefreshToken.builder()
                .userId(userId)
                .token(token)
                .expiryDate(LocalDateTime.now().plusDays(validDays))
                .createdAt(LocalDateTime.now())
                .build();
    }

    /**
     * 도메인 로직: 만료 여부 확인
     */
    public boolean isExpired() {
        return LocalDateTime.now().isAfter(this.expiryDate);
    }

    /**
     * 도메인 로직: 만료까지 남은 시간 (시간 단위)
     */
    public long getHoursUntilExpiry() {
        return java.time.Duration.between(LocalDateTime.now(), this.expiryDate).toHours();
    }
}
```

**설계 포인트**:
- `userId`로 User와 연관 (외래 키)
- `isExpired()`: 만료 여부 체크 (도메인 로직)
- `create()`: 팩토리 메서드로 생성 로직 캡슐화

---

### 1-3. RefreshTokenRepository (MyBatis Mapper)

**파일**: `src/main/java/com/mapo/palantier/user/domain/RefreshTokenRepository.java`

```java
package com.mapo.palantier.user.domain;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import java.util.Optional;

@Mapper
public interface RefreshTokenRepository {

    /**
     * 리프레시 토큰 저장
     */
    void save(RefreshToken refreshToken);

    /**
     * 토큰 문자열로 조회
     */
    Optional<RefreshToken> findByToken(@Param("token") String token);

    /**
     * 사용자 ID로 조회 (1명당 1개의 토큰)
     */
    Optional<RefreshToken> findByUserId(@Param("userId") Long userId);

    /**
     * 사용자 ID로 토큰 삭제 (로그아웃 시 사용)
     */
    void deleteByUserId(@Param("userId") Long userId);

    /**
     * 토큰 문자열로 삭제
     */
    void deleteByToken(@Param("token") String token);

    /**
     * 만료된 토큰 일괄 삭제 (배치 작업용)
     */
    int deleteExpiredTokens();
}
```

---

### 1-4. MyBatis XML Mapper

**파일**: `src/main/resources/mybatis/mapper/RefreshTokenMapper.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.user.domain.RefreshTokenRepository">

    <!-- ResultMap -->
    <resultMap id="refreshTokenResultMap" type="com.mapo.palantier.user.domain.RefreshToken">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="token" column="token"/>
        <result property="expiryDate" column="expiry_date"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 리프레시 토큰 저장 -->
    <insert id="save" parameterType="com.mapo.palantier.user.domain.RefreshToken"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO refresh_tokens (user_id, token, expiry_date, created_at)
        VALUES (#{userId}, #{token}, #{expiryDate}, #{createdAt})
    </insert>

    <!-- 토큰 문자열로 조회 -->
    <select id="findByToken" resultMap="refreshTokenResultMap">
        SELECT id, user_id, token, expiry_date, created_at
        FROM refresh_tokens
        WHERE token = #{token}
    </select>

    <!-- 사용자 ID로 조회 -->
    <select id="findByUserId" resultMap="refreshTokenResultMap">
        SELECT id, user_id, token, expiry_date, created_at
        FROM refresh_tokens
        WHERE user_id = #{userId}
    </select>

    <!-- 사용자 ID로 삭제 -->
    <delete id="deleteByUserId">
        DELETE FROM refresh_tokens
        WHERE user_id = #{userId}
    </delete>

    <!-- 토큰 문자열로 삭제 -->
    <delete id="deleteByToken">
        DELETE FROM refresh_tokens
        WHERE token = #{token}
    </delete>

    <!-- 만료된 토큰 일괄 삭제 -->
    <delete id="deleteExpiredTokens">
        DELETE FROM refresh_tokens
        WHERE expiry_date < NOW()
    </delete>

</mapper>
```

---

## Phase 2: JWT 토큰 제공자 확장

### 2-1. application.yml 수정

**파일**: `src/main/resources/application.yml`

```yaml
jwt:
  secret: your-256-bit-secret-key-change-this-in-production-please-make-it-long-enough
  access-token-expiration: 3600000   # 1시간 (밀리초)
  refresh-token-expiration: 604800000 # 7일 (밀리초)
```

**변경 사항**:
- `expiration` → `access-token-expiration` (1시간)
- `refresh-token-expiration` 추가 (7일)

---

### 2-2. JwtTokenProvider 수정

**파일**: `src/main/java/com/mapo/palantier/config/JwtTokenProvider.java`

```java
package com.mapo.palantier.config;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.security.Keys;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import javax.crypto.SecretKey;
import java.nio.charset.StandardCharsets;
import java.util.Date;

@Component
public class JwtTokenProvider {

    private final SecretKey secretKey;
    private final long accessTokenExpiration;   // 액세스 토큰 만료 시간
    private final long refreshTokenExpiration;  // 리프레시 토큰 만료 시간

    public JwtTokenProvider(
            @Value("${jwt.secret}") String secret,
            @Value("${jwt.access-token-expiration}") long accessTokenExpiration,
            @Value("${jwt.refresh-token-expiration}") long refreshTokenExpiration
    ) {
        this.secretKey = Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
        this.accessTokenExpiration = accessTokenExpiration;
        this.refreshTokenExpiration = refreshTokenExpiration;
    }

    /**
     * 액세스 토큰 생성
     * - 짧은 만료 시간 (1시간)
     * - API 요청 시 사용
     */
    public String generateAccessToken(String email, String role) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + accessTokenExpiration);

        return Jwts.builder()
                .subject(email)
                .claim("role", role)
                .claim("type", "access")  // 토큰 타입 구분
                .issuedAt(now)
                .expiration(expiryDate)
                .signWith(secretKey)
                .compact();
    }

    /**
     * 리프레시 토큰 생성
     * - 긴 만료 시간 (7일)
     * - 액세스 토큰 재발급 시에만 사용
     */
    public String generateRefreshToken(String email) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + refreshTokenExpiration);

        return Jwts.builder()
                .subject(email)
                .claim("type", "refresh")  // 토큰 타입 구분
                .issuedAt(now)
                .expiration(expiryDate)
                .signWith(secretKey)
                .compact();
    }

    /**
     * JWT 토큰에서 이메일 추출
     */
    public String getEmailFromToken(String token) {
        Claims claims = Jwts.parser()
                .verifyWith(secretKey)
                .build()
                .parseSignedClaims(token)
                .getPayload();

        return claims.getSubject();
    }

    /**
     * JWT 토큰에서 역할 추출 (액세스 토큰만 해당)
     */
    public String getRoleFromToken(String token) {
        Claims claims = Jwts.parser()
                .verifyWith(secretKey)
                .build()
                .parseSignedClaims(token)
                .getPayload();

        return claims.get("role", String.class);
    }

    /**
     * JWT 토큰에서 타입 추출 (access or refresh)
     */
    public String getTokenType(String token) {
        Claims claims = Jwts.parser()
                .verifyWith(secretKey)
                .build()
                .parseSignedClaims(token)
                .getPayload();

        return claims.get("type", String.class);
    }

    /**
     * JWT 토큰 유효성 검증
     */
    public boolean validateToken(String token) {
        try {
            Jwts.parser()
                    .verifyWith(secretKey)
                    .build()
                    .parseSignedClaims(token);
            return true;
        } catch (Exception e) {
            return false;
        }
    }

    /**
     * 액세스 토큰인지 확인
     */
    public boolean isAccessToken(String token) {
        return "access".equals(getTokenType(token));
    }

    /**
     * 리프레시 토큰인지 확인
     */
    public boolean isRefreshToken(String token) {
        return "refresh".equals(getTokenType(token));
    }
}
```

**변경 사항**:
- `generateToken()` → `generateAccessToken()`, `generateRefreshToken()` 분리
- `type` claim 추가 (토큰 타입 구분)
- `isAccessToken()`, `isRefreshToken()` 추가

---

## Phase 3: 서비스 레이어 구현

### 3-1. ErrorCode 추가

**파일**: `src/main/java/com/mapo/palantier/common/exception/ErrorCode.java`

```java
public enum ErrorCode {
    // 기존 코드...

    // 토큰 관련 (401)
    INVALID_TOKEN("INVALID_TOKEN", "유효하지 않은 토큰입니다", HttpStatus.UNAUTHORIZED),
    EXPIRED_TOKEN("EXPIRED_TOKEN", "만료된 토큰입니다", HttpStatus.UNAUTHORIZED),
    INVALID_REFRESH_TOKEN("INVALID_REFRESH_TOKEN", "유효하지 않은 리프레시 토큰입니다", HttpStatus.UNAUTHORIZED),
    EXPIRED_REFRESH_TOKEN("EXPIRED_REFRESH_TOKEN", "만료된 리프레시 토큰입니다. 다시 로그인해주세요.", HttpStatus.UNAUTHORIZED),

    // ...
}
```

---

### 3-2. 커스텀 예외 추가

**파일**: `src/main/java/com/mapo/palantier/common/exception/InvalidTokenException.java`

```java
package com.mapo.palantier.common.exception;

import lombok.Getter;

/**
 * 토큰 관련 예외
 * - 유효하지 않은 토큰
 * - 만료된 토큰
 * - 잘못된 형식의 토큰
 */
@Getter
public class InvalidTokenException extends RuntimeException {

    private final ErrorCode errorCode;

    public InvalidTokenException(ErrorCode errorCode) {
        super(errorCode.getMessage());
        this.errorCode = errorCode;
    }

    public InvalidTokenException(ErrorCode errorCode, Throwable cause) {
        super(errorCode.getMessage(), cause);
        this.errorCode = errorCode;
    }
}
```

---

### 3-3. AuthService 수정

**파일**: `src/main/java/com/mapo/palantier/user/application/AuthService.java`

```java
package com.mapo.palantier.user.application;

import com.mapo.palantier.common.exception.*;
import com.mapo.palantier.config.JwtTokenProvider;
import com.mapo.palantier.user.domain.RefreshToken;
import com.mapo.palantier.user.domain.RefreshTokenRepository;
import com.mapo.palantier.user.domain.User;
import com.mapo.palantier.user.domain.UserRepository;
import com.mapo.palantier.user.presentation.dto.TokenResponse;
import lombok.RequiredArgsConstructor;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@RequiredArgsConstructor
public class AuthService {

    private final UserRepository userRepository;
    private final RefreshTokenRepository refreshTokenRepository;
    private final PasswordEncoder passwordEncoder;
    private final JwtTokenProvider jwtTokenProvider;

    private static final int REFRESH_TOKEN_VALIDITY_DAYS = 7;

    @Transactional
    public User signup(String email, String password, String username) {
        // 1. 이메일 중복 체크
        if (userRepository.existsByEmail(email)) {
            throw new DuplicateEmailException(ErrorCode.DUPLICATE_EMAIL);
        }

        // 2. 비밀번호 암호화
        String encodedPassword = passwordEncoder.encode(password);

        // 3. User 도메인 객체 생성
        User newUser = User.createNewUser(email, encodedPassword, username);

        // 4. 저장
        userRepository.save(newUser);

        return newUser;
    }

    /**
     * 로그인 (액세스 토큰 + 리프레시 토큰 발급)
     */
    @Transactional
    public TokenResponse login(String email, String password) {
        // 1. 이메일로 사용자 조회
        User user = userRepository.findByEmail(email)
                .orElseThrow(() -> new AuthenticationException(ErrorCode.INVALID_CREDENTIALS));

        // 2. 비밀번호 검증
        if (!passwordEncoder.matches(password, user.getPassword())) {
            throw new AuthenticationException(ErrorCode.INVALID_CREDENTIALS);
        }

        // 3. 계정 활성화 여부 확인
        if (!user.getIsActive()) {
            throw new AccountInactiveException(ErrorCode.ACCOUNT_INACTIVE);
        }

        // 4. 액세스 토큰 생성
        String accessToken = jwtTokenProvider.generateAccessToken(user.getEmail(), user.getRole().name());

        // 5. 리프레시 토큰 생성
        String refreshToken = jwtTokenProvider.generateRefreshToken(user.getEmail());

        // 6. 기존 리프레시 토큰 삭제 (1명당 1개만 유지)
        refreshTokenRepository.deleteByUserId(user.getId());

        // 7. 새 리프레시 토큰 DB에 저장
        RefreshToken refreshTokenEntity = RefreshToken.create(
                user.getId(),
                refreshToken,
                REFRESH_TOKEN_VALIDITY_DAYS
        );
        refreshTokenRepository.save(refreshTokenEntity);

        // 8. 두 토큰 모두 반환
        return TokenResponse.of(accessToken, refreshToken);
    }

    /**
     * 액세스 토큰 재발급
     */
    @Transactional
    public String refreshAccessToken(String refreshToken) {
        // 1. JWT 서명 검증
        if (!jwtTokenProvider.validateToken(refreshToken)) {
            throw new InvalidTokenException(ErrorCode.INVALID_REFRESH_TOKEN);
        }

        // 2. 리프레시 토큰인지 확인
        if (!jwtTokenProvider.isRefreshToken(refreshToken)) {
            throw new InvalidTokenException(ErrorCode.INVALID_REFRESH_TOKEN);
        }

        // 3. DB에서 리프레시 토큰 조회
        RefreshToken tokenEntity = refreshTokenRepository.findByToken(refreshToken)
                .orElseThrow(() -> new InvalidTokenException(ErrorCode.INVALID_REFRESH_TOKEN));

        // 4. 만료 여부 확인
        if (tokenEntity.isExpired()) {
            // 만료된 토큰 삭제
            refreshTokenRepository.deleteByToken(refreshToken);
            throw new InvalidTokenException(ErrorCode.EXPIRED_REFRESH_TOKEN);
        }

        // 5. 사용자 조회
        User user = userRepository.findById(tokenEntity.getUserId())
                .orElseThrow(() -> new UserNotFoundException(ErrorCode.USER_NOT_FOUND));

        // 6. 새 액세스 토큰 발급
        return jwtTokenProvider.generateAccessToken(user.getEmail(), user.getRole().name());
    }

    /**
     * 로그아웃 (리프레시 토큰 무효화)
     */
    @Transactional
    public void logout(Long userId) {
        refreshTokenRepository.deleteByUserId(userId);
    }

    @Transactional(readOnly = true)
    public User getUserByEmail(String email) {
        return userRepository.findByEmail(email)
                .orElseThrow(() -> new UserNotFoundException(ErrorCode.USER_NOT_FOUND));
    }
}
```

**변경 사항**:
- `login()`: `String` 반환 → `TokenResponse` 반환 (액세스 + 리프레시 토큰)
- `refreshAccessToken()`: 새로운 메서드 추가
- `logout()`: 새로운 메서드 추가

---

## Phase 4: API 엔드포인트 추가

### 4-1. DTO 생성

#### TokenResponse.java
**파일**: `src/main/java/com/mapo/palantier/user/presentation/dto/TokenResponse.java`

```java
package com.mapo.palantier.user.presentation.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
@AllArgsConstructor
public class TokenResponse {
    private String accessToken;
    private String refreshToken;

    public static TokenResponse of(String accessToken, String refreshToken) {
        return TokenResponse.builder()
                .accessToken(accessToken)
                .refreshToken(refreshToken)
                .build();
    }
}
```

#### RefreshTokenRequest.java
**파일**: `src/main/java/com/mapo/palantier/user/presentation/dto/RefreshTokenRequest.java`

```java
package com.mapo.palantier.user.presentation.dto;

import lombok.Getter;
import lombok.NoArgsConstructor;

@Getter
@NoArgsConstructor
public class RefreshTokenRequest {
    private String refreshToken;
}
```

#### AccessTokenResponse.java
**파일**: `src/main/java/com/mapo/palantier/user/presentation/dto/AccessTokenResponse.java`

```java
package com.mapo.palantier.user.presentation.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
@AllArgsConstructor
public class AccessTokenResponse {
    private String accessToken;

    public static AccessTokenResponse of(String accessToken) {
        return AccessTokenResponse.builder()
                .accessToken(accessToken)
                .build();
    }
}
```

---

### 4-2. LoginResponse 수정

**파일**: `src/main/java/com/mapo/palantier/user/presentation/dto/LoginResponse.java`

```java
package com.mapo.palantier.user.presentation.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Getter;

@Getter
@Builder
@AllArgsConstructor
public class LoginResponse {
    private String accessToken;
    private String refreshToken;  // 추가
    private String email;
    private String username;
    private String role;

    public static LoginResponse of(
            String accessToken,
            String refreshToken,  // 추가
            String email,
            String username,
            String role
    ) {
        return LoginResponse.builder()
                .accessToken(accessToken)
                .refreshToken(refreshToken)
                .email(email)
                .username(username)
                .role(role)
                .build();
    }
}
```

---

### 4-3. AuthController 수정

**파일**: `src/main/java/com/mapo/palantier/user/presentation/AuthController.java`

```java
package com.mapo.palantier.user.presentation;

import com.mapo.palantier.user.application.AuthService;
import com.mapo.palantier.user.domain.User;
import com.mapo.palantier.user.presentation.dto.*;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.tags.Tag;
import lombok.RequiredArgsConstructor;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.security.core.userdetails.UserDetails;
import org.springframework.web.bind.annotation.*;

@Tag(name = "인증", description = "회원가입, 로그인, 토큰 재발급 등 인증 관련 API")
@RestController
@RequestMapping("/api/auth")
@RequiredArgsConstructor
public class AuthController {

    private final AuthService authService;

    @Operation(summary = "회원가입", description = "새로운 사용자를 등록합니다.")
    @PostMapping("/signup")
    public ResponseEntity<SignupResponse> signup(@RequestBody SignupRequest request) {
        User user = authService.signup(
                request.getEmail(),
                request.getPassword(),
                request.getUsername()
        );

        return ResponseEntity
                .status(HttpStatus.CREATED)
                .body(SignupResponse.from(user));
    }

    @Operation(
        summary = "로그인",
        description = "이메일과 비밀번호로 로그인하여 액세스 토큰과 리프레시 토큰을 발급받습니다."
    )
    @PostMapping("/login")
    public ResponseEntity<LoginResponse> login(@RequestBody LoginRequest request) {
        // 1. 로그인 처리 및 토큰 발급
        TokenResponse tokens = authService.login(request.getEmail(), request.getPassword());

        // 2. 사용자 정보 조회
        User user = authService.getUserByEmail(request.getEmail());

        // 3. 응답 생성
        LoginResponse response = LoginResponse.of(
                tokens.getAccessToken(),
                tokens.getRefreshToken(),
                user.getEmail(),
                user.getUsername(),
                user.getRole().name()
        );

        return ResponseEntity.ok(response);
    }

    @Operation(
        summary = "액세스 토큰 재발급",
        description = "리프레시 토큰을 사용하여 새로운 액세스 토큰을 발급받습니다."
    )
    @PostMapping("/refresh")
    public ResponseEntity<AccessTokenResponse> refresh(@RequestBody RefreshTokenRequest request) {
        String newAccessToken = authService.refreshAccessToken(request.getRefreshToken());
        return ResponseEntity.ok(AccessTokenResponse.of(newAccessToken));
    }

    @Operation(
        summary = "로그아웃",
        description = "리프레시 토큰을 무효화하여 로그아웃합니다."
    )
    @PostMapping("/logout")
    public ResponseEntity<Void> logout(@AuthenticationPrincipal UserDetails userDetails) {
        // UserDetails에서 이메일 추출 후 사용자 조회
        User user = authService.getUserByEmail(userDetails.getUsername());
        authService.logout(user.getId());
        return ResponseEntity.ok().build();
    }
}
```

---

## Phase 5: 예외 처리 및 보안 강화

### 5-1. GlobalExceptionHandler 수정

**파일**: `src/main/java/com/mapo/palantier/common/exception/GlobalExceptionHandler.java`

```java
/**
 * 토큰 관련 예외 처리
 * HTTP Status: 401 Unauthorized
 */
@ExceptionHandler(InvalidTokenException.class)
public ResponseEntity<ErrorResponse> handleInvalidTokenException(
        InvalidTokenException e,
        HttpServletRequest request
) {
    log.warn("Invalid token: {} - Path: {}", e.getMessage(), request.getRequestURI());

    ErrorResponse errorResponse = ErrorResponse.of(
            e.getErrorCode().getCode(),
            e.getMessage(),
            request.getRequestURI()
    );

    return ResponseEntity
            .status(e.getErrorCode().getHttpStatus())
            .body(errorResponse);
}
```

---

### 5-2. 만료된 토큰 정리 배치 작업 (선택 사항)

**파일**: `src/main/java/com/mapo/palantier/config/ScheduledTasks.java`

```java
package com.mapo.palantier.config;

import com.mapo.palantier.user.domain.RefreshTokenRepository;
import lombok.RequiredArgsConstructor;
import lombok.extern.slf4j.Slf4j;
import org.springframework.scheduling.annotation.Scheduled;
import org.springframework.stereotype.Component;

@Slf4j
@Component
@RequiredArgsConstructor
public class ScheduledTasks {

    private final RefreshTokenRepository refreshTokenRepository;

    /**
     * 만료된 리프레시 토큰 정리
     * - 매일 새벽 3시 실행
     */
    @Scheduled(cron = "0 0 3 * * *")
    public void cleanupExpiredRefreshTokens() {
        log.info("Starting cleanup of expired refresh tokens...");
        int deletedCount = refreshTokenRepository.deleteExpiredTokens();
        log.info("Deleted {} expired refresh tokens", deletedCount);
    }
}
```

**활성화 방법**:
```java
// Application.java에 @EnableScheduling 추가
@SpringBootApplication
@EnableScheduling
public class PalantierApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(PalantierApiApplication.class, args);
    }
}
```

---

## Phase 6: 테스트 및 검증

### 6-1. API 테스트 시나리오

#### 시나리오 1: 로그인
```bash
POST /api/auth/login
{
  "email": "test@test.com",
  "password": "password123"
}

# 응답
{
  "accessToken": "eyJhbG...",
  "refreshToken": "eyJhbG...",
  "email": "test@test.com",
  "username": "테스터",
  "role": "ROLE_USER"
}
```

#### 시나리오 2: 액세스 토큰으로 API 호출
```bash
GET /api/some-protected-resource
Authorization: Bearer eyJhbG... (액세스 토큰)

# 응답: 200 OK
```

#### 시나리오 3: 액세스 토큰 만료 후 재발급
```bash
POST /api/auth/refresh
{
  "refreshToken": "eyJhbG..."
}

# 응답
{
  "accessToken": "eyJhbG... (새 토큰)"
}
```

#### 시나리오 4: 로그아웃
```bash
POST /api/auth/logout
Authorization: Bearer eyJhbG... (액세스 토큰)

# 응답: 200 OK
# DB에서 리프레시 토큰 삭제됨
```

#### 시나리오 5: 로그아웃 후 리프레시 시도 (실패)
```bash
POST /api/auth/refresh
{
  "refreshToken": "eyJhbG..."
}

# 응답: 401 Unauthorized
{
  "errorCode": "INVALID_REFRESH_TOKEN",
  "message": "유효하지 않은 리프레시 토큰입니다",
  ...
}
```

---

### 6-2. DB 검증

```sql
-- 로그인 후 리프레시 토큰 저장 확인
SELECT * FROM refresh_tokens WHERE user_id = 1;

-- 로그아웃 후 토큰 삭제 확인
SELECT * FROM refresh_tokens WHERE user_id = 1;
-- (결과 없음)

-- 만료된 토큰 조회
SELECT * FROM refresh_tokens WHERE expiry_date < NOW();
```

---

## 구현 체크리스트

### Phase 1: 데이터베이스 및 도메인
- [ ] `V3__create_refresh_tokens_table.sql` 마이그레이션 작성
- [ ] `RefreshToken` 도메인 엔티티 생성
- [ ] `RefreshTokenRepository` 인터페이스 작성
- [ ] `RefreshTokenMapper.xml` 작성

### Phase 2: JWT 확장
- [ ] `application.yml`에 리프레시 토큰 설정 추가
- [ ] `JwtTokenProvider` 수정 (액세스/리프레시 분리)

### Phase 3: 서비스 레이어
- [ ] `ErrorCode`에 토큰 관련 에러 추가
- [ ] `InvalidTokenException` 생성
- [ ] `AuthService.login()` 수정
- [ ] `AuthService.refreshAccessToken()` 추가
- [ ] `AuthService.logout()` 추가

### Phase 4: API 엔드포인트
- [ ] `TokenResponse` DTO 생성
- [ ] `RefreshTokenRequest` DTO 생성
- [ ] `AccessTokenResponse` DTO 생성
- [ ] `LoginResponse` 수정
- [ ] `AuthController` 수정 (`/refresh`, `/logout` 추가)

### Phase 5: 예외 처리
- [ ] `GlobalExceptionHandler`에 `InvalidTokenException` 핸들러 추가
- [ ] (선택) 만료 토큰 정리 스케줄러 추가

### Phase 6: 테스트
- [ ] 로그인 테스트
- [ ] 액세스 토큰으로 API 호출 테스트
- [ ] 리프레시 토큰으로 재발급 테스트
- [ ] 로그아웃 테스트
- [ ] 만료된 토큰 예외 처리 테스트

---

## 예상 소요 시간

| Phase | 작업 | 예상 시간 |
|-------|------|----------|
| Phase 1 | DB 및 도메인 모델 | 1~2시간 |
| Phase 2 | JWT 확장 | 30분 |
| Phase 3 | 서비스 레이어 | 1~2시간 |
| Phase 4 | API 엔드포인트 | 1시간 |
| Phase 5 | 예외 처리 | 30분 |
| Phase 6 | 테스트 | 1~2시간 |
| **총계** | | **5~8시간** |

---

## 보안 고려 사항

### 1. 리프레시 토큰 저장 위치 (클라이언트)

**권장**: HttpOnly Cookie
```java
// AuthController에서 쿠키로 리프레시 토큰 전달
ResponseCookie cookie = ResponseCookie.from("refreshToken", refreshToken)
        .httpOnly(true)
        .secure(true)  // HTTPS에서만 전송
        .path("/")
        .maxAge(7 * 24 * 60 * 60)  // 7일
        .sameSite("Strict")
        .build();

response.addHeader("Set-Cookie", cookie.toString());
```

**비권장**: localStorage (XSS 공격 위험)

### 2. 리프레시 토큰 로테이션 (선택 사항)

리프레시 토큰 사용 시마다 새로운 리프레시 토큰 발급:
```java
public TokenResponse refreshAccessToken(String oldRefreshToken) {
    // ... 검증 로직 ...

    // 새 액세스 토큰 + 새 리프레시 토큰 발급
    String newAccessToken = jwtTokenProvider.generateAccessToken(...);
    String newRefreshToken = jwtTokenProvider.generateRefreshToken(...);

    // 기존 리프레시 토큰 삭제 후 새 토큰 저장
    refreshTokenRepository.deleteByToken(oldRefreshToken);
    refreshTokenRepository.save(RefreshToken.create(..., newRefreshToken, ...));

    return TokenResponse.of(newAccessToken, newRefreshToken);
}
```

### 3. 동시 로그인 제한

현재 구현: 1명당 1개의 리프레시 토큰 (새 로그인 시 기존 토큰 삭제)

여러 기기 허용 시:
```java
// deleteByUserId() 대신 추가 저장
refreshTokenRepository.save(newToken);

// 단, 최대 개수 제한 필요 (예: 3개)
List<RefreshToken> tokens = refreshTokenRepository.findAllByUserId(userId);
if (tokens.size() >= 3) {
    // 가장 오래된 토큰 삭제
    refreshTokenRepository.delete(tokens.get(0));
}
```

---

## 마이그레이션 전략

### 기존 사용자 영향 최소화

1. **호환 모드 운영** (선택 사항)
   - 처음에는 액세스 토큰만 사용하는 구 방식도 허용
   - 점진적으로 리프레시 토큰 방식으로 전환

2. **공지**
   - "로그인 유지 시간이 늘어납니다" (사용자 경험 개선)
   - 재로그인 필요 (토큰 형식 변경)

3. **배포 순서**
   - DB 마이그레이션 먼저 실행
   - 애플리케이션 배포
   - 기존 토큰 만료 시 자연스럽게 새 방식 적용

---

## 요약

**목표**: 액세스 토큰(1시간) + 리프레시 토큰(7일) 구조로 보안성과 사용자 경험 모두 확보

**핵심 변경 사항**:
1. DB에 `refresh_tokens` 테이블 추가
2. `JwtTokenProvider`에서 두 종류의 토큰 생성
3. 로그인 시 두 토큰 모두 반환
4. `/api/auth/refresh` 엔드포인트 추가
5. `/api/auth/logout` 엔드포인트 추가

**예상 소요 시간**: 5~8시간

**다음 단계**: Phase 1부터 순차적으로 구현 시작!
