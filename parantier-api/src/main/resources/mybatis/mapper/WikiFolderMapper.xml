<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.wiki.folder.WikiFolderMapper">

    <resultMap id="WikiFolderResultMap" type="com.mapo.palantier.wiki.folder.WikiFolder">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findAll" resultMap="WikiFolderResultMap">
        SELECT id, parent_id, name, sort_order, created_by, created_at, updated_at
        FROM wiki_folders
        WHERE deleted_at IS NULL
        ORDER BY parent_id NULLS FIRST, sort_order
    </select>

    <select id="findById" resultMap="WikiFolderResultMap">
        SELECT id, parent_id, name, sort_order, created_by, created_at, updated_at
        FROM wiki_folders
        WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO wiki_folders (parent_id, name, sort_order, created_by)
        VALUES (#{parentId}, #{name}, #{sortOrder}, #{createdBy})
    </insert>

    <update id="update">
        UPDATE wiki_folders
        SET name       = #{name},
            parent_id  = #{parentId},
            sort_order = #{sortOrder},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <update id="softDelete">
        UPDATE wiki_folders
        SET deleted_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
