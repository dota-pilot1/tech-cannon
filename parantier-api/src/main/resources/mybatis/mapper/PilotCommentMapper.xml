<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.pilot.comment.PilotCommentMapper">

    <resultMap id="PilotCommentResultMap" type="com.mapo.palantier.pilot.comment.PilotComment">
        <id property="id" column="id"/>
        <result property="postId" column="post_id"/>
        <result property="authorId" column="author_id"/>
        <result property="authorName" column="author_name"/>
        <result property="content" column="content"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <select id="findByPostId" resultMap="PilotCommentResultMap">
        SELECT c.id, c.post_id, c.author_id,
               u.username as author_name,
               c.content, c.created_at, c.updated_at
        FROM pilot_comments c
        JOIN users u ON c.author_id = u.id
        WHERE c.post_id = #{postId}
          AND c.deleted_at IS NULL
        ORDER BY c.created_at ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pilot_comments (post_id, author_id, content)
        VALUES (#{postId}, #{authorId}, #{content})
    </insert>

    <update id="softDelete">
        UPDATE pilot_comments
        SET deleted_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
