<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.figma.infrastructure.FigmaCategoryMapper">

    <resultMap id="FigmaCategoryResultMap" type="com.mapo.palantier.figma.domain.FigmaCategory">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="icon" column="icon"/>
        <result property="emoji" column="emoji"/>
        <result property="orderNum" column="order_num"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="FigmaCategoryResultMap">
        SELECT id, name, icon, emoji, order_num, is_active, created_at, updated_at
        FROM figma_categories
        WHERE is_active = true
        ORDER BY order_num ASC, id ASC
    </select>

    <select id="findById" resultMap="FigmaCategoryResultMap">
        SELECT id, name, icon, emoji, order_num, is_active, created_at, updated_at
        FROM figma_categories
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO figma_categories (name, icon, emoji, order_num, is_active)
        VALUES (#{name}, #{icon}, #{emoji}, #{orderNum}, #{isActive})
    </insert>

    <update id="update">
        UPDATE figma_categories
        SET name       = #{name},
            icon       = #{icon},
            emoji      = #{emoji},
            order_num  = #{orderNum},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM figma_categories
        WHERE id = #{id}
    </delete>

    <update id="updateOrderNum">
        UPDATE figma_categories
        SET order_num  = #{orderNum},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
