<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapo.palantier.subutai.ai.infrastructure.SubutaiGithubFolderMapper">

    <resultMap id="FolderResultMap" type="com.mapo.palantier.subutai.ai.domain.SubutaiGithubFolder">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="orderNum" column="order_num"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findAll" resultMap="FolderResultMap">
        SELECT id, name, order_num, created_by, created_at
        FROM subutai_github_folders
        ORDER BY order_num ASC, id ASC
    </select>

    <select id="findById" resultMap="FolderResultMap">
        SELECT id, name, order_num, created_by, created_at
        FROM subutai_github_folders WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO subutai_github_folders (name, order_num, created_by)
        VALUES (#{name}, #{orderNum}, #{createdBy})
    </insert>

    <delete id="delete">
        DELETE FROM subutai_github_folders WHERE id = #{id}
    </delete>

</mapper>
