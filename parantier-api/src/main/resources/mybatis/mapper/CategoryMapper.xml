<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.authority.domain.CategoryRepository">

    <!-- 모든 카테고리 조회 -->
    <select id="findAll" resultType="com.mapo.palantier.authority.domain.Category">
        SELECT id, name, description, created_at
        FROM category
        ORDER BY created_at DESC
    </select>

    <!-- ID로 카테고리 조회 -->
    <select id="findById" resultType="com.mapo.palantier.authority.domain.Category">
        SELECT id, name, description, created_at
        FROM category
        WHERE id = #{id}
    </select>

    <!-- 이름으로 카테고리 조회 -->
    <select id="findByName" resultType="com.mapo.palantier.authority.domain.Category">
        SELECT id, name, description, created_at
        FROM category
        WHERE name = #{name}
    </select>

    <!-- 카테고리 생성 -->
    <insert id="create" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, description, created_at)
        VALUES (#{name}, #{description}, NOW())
    </insert>

    <!-- 카테고리 수정 -->
    <update id="update">
        UPDATE category
        SET name = #{name},
            description = #{description}
        WHERE id = #{id}
    </update>

    <!-- 카테고리 삭제 -->
    <delete id="delete">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>
