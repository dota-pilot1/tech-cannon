<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapo.palantier.figma.infrastructure.FigmaLinkMapper">

    <resultMap id="FigmaLinkResultMap" type="com.mapo.palantier.figma.domain.FigmaLink">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="title" column="title"/>
        <result property="url" column="url"/>
        <result property="orderNum" column="order_num"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByCategoryId" resultMap="FigmaLinkResultMap">
        SELECT id, category_id, title, url, order_num, created_at
        FROM figma_links
        WHERE category_id = #{categoryId}
        ORDER BY order_num ASC, id ASC
    </select>

    <select id="findById" resultMap="FigmaLinkResultMap">
        SELECT id, category_id, title, url, order_num, created_at
        FROM figma_links
        WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO figma_links (category_id, title, url, order_num)
        VALUES (#{categoryId}, #{title}, #{url}, #{orderNum})
    </insert>

    <update id="update">
        UPDATE figma_links
        SET title     = #{title},
            url       = #{url},
            order_num = #{orderNum}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM figma_links
        WHERE id = #{id}
    </delete>

    <update id="updateOrderNum">
        UPDATE figma_links
        SET order_num = #{orderNum}
        WHERE id = #{id}
    </update>

</mapper>
