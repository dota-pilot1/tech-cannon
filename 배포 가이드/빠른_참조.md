# 배포 빠른 참조

## 인프라 정보

### EC2
- **IP**: `43.200.241.26`
- **도메인**: `api.dxline-tallent.com`
- **PEM 파일**: `/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem`
- **사용자**: `ubuntu`

### S3 & CloudFront
- **S3 버킷**: `dxline-tallent-front`
- **CloudFront ID**: `E11NF3HMOB52NI`
- **도메인**: `https://dxline-tallent.com`

### PostgreSQL
- **Host**: `43.200.241.26` (EC2 내부)
- **Port**: `5432`
- **Database**: `palantier`
- **Username**: `palantier_user`
- **Password**: `palantier_password`

---

## 프론트엔드 배포 (한 번에)

```bash
cd /Users/terecal/mapo-palantier-project/parantier-front
npm run build
aws s3 sync dist s3://dxline-tallent-front --delete
aws cloudfront create-invalidation --distribution-id E11NF3HMOB52NI --paths "/*"
```

**확인**: https://dxline-tallent.com

---

## 백엔드 배포 (한 번에)

### 1. 빌드

```bash
cd /Users/terecal/mapo-palantier-project/parantier-api
./gradlew clean bootJar
```

### 2. 시작 스크립트 생성

```bash
cat > /tmp/start-backend.sh << 'EOF'
#!/bin/bash
pkill -f 'java -jar' || true
sleep 2

export AWS_ACCESS_KEY_ID=<YOUR_AWS_ACCESS_KEY_ID>
export AWS_SECRET_ACCESS_KEY=<YOUR_AWS_SECRET_ACCESS_KEY>
export AWS_S3_BUCKET_NAME=hibot-docu
export AWS_S3_REGION=ap-northeast-2

nohup java -jar /home/ubuntu/parantier-api-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > /home/ubuntu/app.log 2>&1 &

echo "Application started. PID: $!"
EOF
chmod +x /tmp/start-backend.sh
```

### 3. 업로드 및 실행

```bash
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"

scp -i "$PEM" \
    /Users/terecal/mapo-palantier-project/parantier-api/build/libs/parantier-api-0.0.1-SNAPSHOT.jar \
    ubuntu@43.200.241.26:/home/ubuntu/

scp -i "$PEM" /tmp/start-backend.sh ubuntu@43.200.241.26:/home/ubuntu/

ssh -i "$PEM" ubuntu@43.200.241.26 "bash /home/ubuntu/start-backend.sh"
```

### 4. 확인

```bash
# 헬스체크 (10초 대기 후)
sleep 10
curl http://43.200.241.26:8080/actuator/health

# 로그 확인
ssh -i "$PEM" ubuntu@43.200.241.26 "tail -30 /home/ubuntu/app.log"
```

---

## 자주 사용하는 명령어

### SSH 접속

```bash
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26
```

### 백엔드 로그 확인

```bash
# 실시간 로그
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 "tail -f /home/ubuntu/app.log"

# 최근 50줄
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 "tail -50 /home/ubuntu/app.log"
```

### 백엔드 프로세스 확인

```bash
# 프로세스 확인
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 "ps aux | grep java"

# 프로세스 종료
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 "pkill -f 'java -jar'"
```

### PostgreSQL 접속

```bash
# EC2에서 직접 접속
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26
docker exec -it palantier-postgres psql -U palantier_user -d palantier

# 로컬에서 포트 포워딩으로 접속
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" -L 5433:localhost:5432 ubuntu@43.200.241.26
# 다른 터미널에서:
psql -h localhost -p 5433 -U palantier_user -d palantier
```

### CloudFront 캐시 무효화 확인

```bash
# 최근 무효화 상태 확인
aws cloudfront list-invalidations --distribution-id E11NF3HMOB52NI
```

### S3 버킷 내용 확인

```bash
# 버킷 파일 목록
aws s3 ls s3://dxline-tallent-front --recursive --human-readable --summarize
```

---

## 환경 변수 파일

### 프론트엔드 `.env.production`

```env
VITE_API_URL=https://api.dxline-tallent.com/api
```

### 백엔드 `.env`

```env
# AWS S3 Configuration
AWS_ACCESS_KEY_ID=<YOUR_AWS_ACCESS_KEY_ID>
AWS_SECRET_ACCESS_KEY=<YOUR_AWS_SECRET_ACCESS_KEY>
AWS_S3_BUCKET_NAME=hibot-docu
AWS_S3_REGION=ap-northeast-2
```

**참고**: 실제 AWS 자격증명은 `/Users/terecal/mapo-palantier-project/parantier-api/.env` 파일에 저장되어 있습니다.

---

## 완전 자동 배포 스크립트

### 프론트엔드 + 백엔드 전체 배포

파일: `/Users/terecal/mapo-palantier-project/deploy-all.sh`

```bash
#!/bin/bash
set -e

PROJECT_ROOT="/Users/terecal/mapo-palantier-project"
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"
EC2_HOST="ubuntu@43.200.241.26"

echo "=========================================="
echo "  Palantier 전체 배포 시작"
echo "=========================================="
echo ""

# 프론트엔드 배포
echo "📦 [1/4] 프론트엔드 빌드 중..."
cd "$PROJECT_ROOT/parantier-front"
npm run build

echo "☁️  [2/4] S3 업로드 중..."
aws s3 sync dist s3://dxline-tallent-front --delete

echo "🔄 [3/4] CloudFront 캐시 무효화 중..."
aws cloudfront create-invalidation --distribution-id E11NF3HMOB52NI --paths "/*" > /dev/null

echo "✅ 프론트엔드 배포 완료!"
echo ""

# 백엔드 배포
echo "📦 [4/4] 백엔드 빌드 중..."
cd "$PROJECT_ROOT/parantier-api"
./gradlew clean bootJar

echo "📝 시작 스크립트 생성 중..."
cat > /tmp/start-backend.sh << 'EOF'
#!/bin/bash
pkill -f 'java -jar' || true
sleep 2

export AWS_ACCESS_KEY_ID=<YOUR_AWS_ACCESS_KEY_ID>
export AWS_SECRET_ACCESS_KEY=<YOUR_AWS_SECRET_ACCESS_KEY>
export AWS_S3_BUCKET_NAME=hibot-docu
export AWS_S3_REGION=ap-northeast-2

nohup java -jar /home/ubuntu/parantier-api-0.0.1-SNAPSHOT.jar \
    --spring.profiles.active=prod \
    > /home/ubuntu/app.log 2>&1 &

echo "Application started. PID: $!"
EOF
chmod +x /tmp/start-backend.sh

echo "☁️  EC2 업로드 중..."
scp -i "$PEM" build/libs/parantier-api-0.0.1-SNAPSHOT.jar "$EC2_HOST:/home/ubuntu/"
scp -i "$PEM" /tmp/start-backend.sh "$EC2_HOST:/home/ubuntu/"

echo "▶️  백엔드 시작 중..."
ssh -i "$PEM" "$EC2_HOST" "bash /home/ubuntu/start-backend.sh"

echo "⏳ 애플리케이션 시작 대기 중 (10초)..."
sleep 10

echo "🏥 헬스체크 중..."
HEALTH=$(curl -s http://43.200.241.26:8080/actuator/health)
echo "$HEALTH"

if echo "$HEALTH" | grep -q '"status":"UP"'; then
    echo "✅ 백엔드 배포 완료!"
else
    echo "❌ 백엔드 헬스체크 실패!"
    ssh -i "$PEM" "$EC2_HOST" "tail -30 /home/ubuntu/app.log"
    exit 1
fi

echo ""
echo "=========================================="
echo "  전체 배포 완료!"
echo "=========================================="
echo ""
echo "🌐 프론트엔드: https://dxline-tallent.com"
echo "🌐 백엔드: https://api.dxline-tallent.com"
echo "🏥 헬스체크: http://43.200.241.26:8080/actuator/health"
echo ""
```

### 실행 권한 설정 및 실행

```bash
chmod +x /Users/terecal/mapo-palantier-project/deploy-all.sh
/Users/terecal/mapo-palantier-project/deploy-all.sh
```

---

## 긴급 롤백

### 프론트엔드 롤백

```bash
# 이전 S3 버전 복원 (버킷 버저닝이 활성화된 경우)
aws s3api list-object-versions --bucket dxline-tallent-front --prefix index.html

# 또는 이전 빌드 파일 재배포
cd /Users/terecal/mapo-palantier-project/parantier-front
git checkout <이전_커밋_해시>
npm run build
aws s3 sync dist s3://dxline-tallent-front --delete
aws cloudfront create-invalidation --distribution-id E11NF3HMOB52NI --paths "/*"
```

### 백엔드 롤백

```bash
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"

# 이전 JAR 파일 재배포
cd /Users/terecal/mapo-palantier-project/parantier-api
git checkout <이전_커밋_해시>
./gradlew clean bootJar

# 업로드 및 재시작
scp -i "$PEM" build/libs/parantier-api-0.0.1-SNAPSHOT.jar ubuntu@43.200.241.26:/home/ubuntu/
ssh -i "$PEM" ubuntu@43.200.241.26 "bash /home/ubuntu/start-backend.sh"
```

---

## 모니터링

### 백엔드 헬스체크

```bash
# 간단한 체크
curl http://43.200.241.26:8080/actuator/health

# 지속적 모니터링 (5초마다)
watch -n 5 'curl -s http://43.200.241.26:8080/actuator/health | jq'
```

### 프론트엔드 접속 확인

```bash
# HTTP 상태 코드 확인
curl -I https://dxline-tallent.com

# CloudFront 배포 상태 확인
aws cloudfront get-distribution --id E11NF3HMOB52NI | jq '.Distribution.Status'
```

### 로그 모니터링

```bash
# 백엔드 로그 실시간 모니터링 (에러만)
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 \
    "tail -f /home/ubuntu/app.log | grep -i error"

# PostgreSQL 로그 확인
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 \
    "docker logs palantier-postgres --tail 50"
```

---

## 데이터베이스 관리

### 백업

```bash
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"
DATE=$(date +%Y%m%d_%H%M%S)

# EC2에서 백업 생성
ssh -i "$PEM" ubuntu@43.200.241.26 \
    "docker exec palantier-postgres pg_dump -U palantier_user palantier > /home/ubuntu/backup_$DATE.sql"

# 로컬로 다운로드
scp -i "$PEM" ubuntu@43.200.241.26:/home/ubuntu/backup_$DATE.sql \
    /Users/terecal/mapo-palantier-project/backups/
```

### 복원

```bash
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"
BACKUP_FILE="backup_20260327_120000.sql"

# 백업 파일 업로드
scp -i "$PEM" /Users/terecal/mapo-palantier-project/backups/$BACKUP_FILE \
    ubuntu@43.200.241.26:/home/ubuntu/

# 복원 실행
ssh -i "$PEM" ubuntu@43.200.241.26 \
    "docker exec -i palantier-postgres psql -U palantier_user -d palantier < /home/ubuntu/$BACKUP_FILE"
```

---

**마지막 업데이트**: 2026-03-27
