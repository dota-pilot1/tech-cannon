# 배포 빠른 참조

## 프론트엔드 배포

```bash
cd parantier-front
npm run build
aws s3 sync dist s3://dxline-tallent-front --delete
aws cloudfront create-invalidation --distribution-id E11NF3HMOB52NI --paths "/*"
```

## 백엔드 배포

```bash
cd parantier-api
./gradlew clean bootJar

PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"
scp -i "$PEM" build/libs/parantier-api-0.0.1-SNAPSHOT.jar ubuntu@43.200.241.26:/home/ubuntu/
ssh -i "$PEM" ubuntu@43.200.241.26 "pkill -f 'java -jar' && sleep 2 && nohup java -jar /home/ubuntu/parantier-api-0.0.1-SNAPSHOT.jar --spring.profiles.active=prod > /home/ubuntu/app.log 2>&1 &"
```

## 자주 사용하는 명령어

### SSH 접속
```bash
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26
```

### 로그 확인
```bash
# 백엔드 로그
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26 "tail -f /home/ubuntu/app.log"

# 헬스체크
curl http://43.200.241.26:8080/actuator/health
```

### PostgreSQL 접속
```bash
ssh -i "/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem" ubuntu@43.200.241.26
PGPASSWORD=palantier_password psql -h localhost -p 5432 -U palantier_user -d palantier
```

### DB 백업
```bash
PEM="/Users/terecal/dxline-container/hibot-d-server-key 복사본.pem"
ssh -i "$PEM" ubuntu@43.200.241.26 "PGPASSWORD=palantier_password pg_dump -h localhost -U palantier_user palantier > backup_$(date +%Y%m%d).sql"
```

---

**마지막 업데이트**: 2026-03-28
